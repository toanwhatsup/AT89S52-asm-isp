diff --git a/atzmon/atzmon.asm b/atzmon/atzmon.asm
index 5301d9c..8766ac2 100644
--- a/atzmon/atzmon.asm
+++ b/atzmon/atzmon.asm
@@ -20,11 +20,24 @@ LCD_ADDR .equ 0x4E   ; Change to 0x7E if your backpack uses a different address
     .org  0x0030
 START:
     acall LCD_INIT
-    
-    ; If initialization worked, send 'A'
-    mov   a, #'A'
+
+    mov   a, #0x02      ; Command: Clear Display
+    acall LCD_CMD
+
+    mov   r7, #5        ; Clear needs a long delay
+    acall DELAY         ; Use your R7-based loop if needed
+
+CLEAN_WAIT:
+    acall LCD_DELAY_MS
+    djnz  r7, CLEAN_WAIT
+
+    mov   a, #0x80      ; Command: Force Cursor to Line 1, Pos 0
+    acall LCD_CMD
+    acall LCD_DELAY_MS
+
+    mov   a, #0x41      ; ASCII for 'A'
     acall LCD_DATA
-    
+
     clr   p1.0          ; Success LED
 HERE:
     sjmp  HERE
diff --git a/atzmon/pcf8574_lcd.asm b/atzmon/pcf8574_lcd.asm
index 5b2989d..15f1716 100644
--- a/atzmon/pcf8574_lcd.asm
+++ b/atzmon/pcf8574_lcd.asm
@@ -25,36 +25,38 @@ LCD_PULSE_EN:
 
 ; --- LCD Initialization (The "Magic" Sequence) ---
 LCD_INIT:
-    ; 1. Power-on Wait (>15ms)
-    mov   r7, #20
-INIT_D1: acall LCD_DELAY_MS
-    djnz  r7, INIT_D1
+    ; 1. Long wait for LCD internal reset (>40ms)
+    mov   r7, #50
+INIT_DELAY:
+    acall LCD_DELAY_MS
+    djnz  r7, INIT_DELAY
 
-    ; 2. Force 8-bit mode (Pulse 1)
+    ; 2. Force 8-bit mode (The "Shake Awake" sequense)
+    ; We send 0x30 three times to reset the LCD controller
     mov   a, #0x38      ; D5,D4 high + Backlight
     acall LCD_RAW_WRITE
-    acall LCD_PULSE_EN
-    mov   r7, #5
-INIT_D2: acall LCD_DELAY_MS
-    djnz  r7, INIT_D2
+    acall LCD_PULSE_EN  ; Pulse 1
+    mov   r7, #10
+    djnz  r7, .         ; Small delay
 
-    ; 3. Force 8-bit mode (Pulse 2)
-    acall LCD_PULSE_EN
+    acall LCD_PULSE_EN  ; Pulse 2
+    mov   r7, #10
+    djnz  r7, .
+
+    acall LCD_PULSE_EN  ; Pulse 3
     acall LCD_DELAY_MS
 
-    ; 4. Switch to 4-bit mode (The 0x02 command)
+    ; 3. Switch to 4-bit mode (The 0x02 command)
     mov   a, #0x28      ; D5 high + Backlight
     acall LCD_RAW_WRITE
     acall LCD_PULSE_EN
     acall LCD_DELAY_MS
 
-    ; 5. Now in 4-bit mode, send standard commands
+    ; 4. Final configuration (LCD_CMD for nibbles)
     mov   a, #0x28      ; 4-bit, 2 lines, 5x8 font
     acall LCD_CMD
     mov   a, #0x0C      ; Display ON, Cursor OFF
     acall LCD_CMD
-    mov   a, #0x06      ; Entry mode: Auto-increment
-    acall LCD_CMD
     mov   a, #0x01      ; Clear display
     acall LCD_CMD
     ret
@@ -97,14 +99,16 @@ LCD_DATA:
     acall I2C_DELAY     ; Data writes are faster than commands
     ret
 
-; --- Medium Delay for LCD Setup (~2ms) ---
+; --- Medium Delay for LCD Setup ---
 LCD_DELAY_MS:
     push b
-    mov  b, #2
+    push 06h            ; Save R6
+    mov  b, #2          ; Outer loop
 LD_L1:
-    mov  r6, #250
-    djnz r6, .          ; Tiny internal loop
+    mov  r6, #250       ; Inner loop (~500us at 12MHz)
+    djnz r6, .          ; Jump to self until R6 is 0
     djnz b, LD_L1
+    pop  06h            ; Restore R6
     pop  b
     ret
 
